import React from "react";
import ReactDOM from "react-dom/client";
import WorldPlateApp from "./WorldPlateApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <WorldPlateApp />
  </React.StrictMode>
);
